[build]
  base = "Frontend"
  command = "npm install && npm run build"
  publish = "dist"

[dev]
  command = "cd Frontend && npm run dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
